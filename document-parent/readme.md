Document Post Parent
====================

Creates WordPress metabox for selecting a document's (or other post type's) post parent. Useful for querying for documents on the front-end as you would attachments, e.g., `get_posts( array( 'post_type' => 'document', 'post_parent' => 1 ) )`